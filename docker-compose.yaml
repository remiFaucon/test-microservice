version: "3"
services:
  face:
    build: faceReconnation/
    volumes:
      - "./faceReconnation:/app"
    ports:
      - "5000:5000"

  auth:
    build: auth/
    volumes:
      - "./auth:/app"
    ports:
      - "3002:3002"
